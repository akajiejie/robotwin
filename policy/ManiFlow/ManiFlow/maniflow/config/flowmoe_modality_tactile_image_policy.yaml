defaults:
  - task: demo_task_tactile

name: train_maniflow_modality_moe_tactile_image_timm_policy

task_name: ${task.name}
shape_meta: ${task.shape_meta}
exp_name: "debug"

horizon: 16
n_obs_steps: 2
n_action_steps: 16
n_latency_steps: 0
dataset_obs_steps: ${n_obs_steps}
keypoint_visible_rate: 1.0
obs_as_global_cond: true

policy:
  _target_: maniflow.policy.maniflow_image_policy.ManiFlowTransformerImagePolicy
  block_type: "DiTXMoE"  # 使用DiTXMoE (模态级别MoE)
  n_layer: 12
  n_head: 8
  n_emb: 768
  
  # 🔥 视觉条件长度配置 (output_token_sequence=true + avg模式)
  # TimmMultimodalEncoder + output_token_sequence=true + feature_aggregation='avg' 输出:
  #   ✅ RGB相机 (SigLIP vit_base_patch16_224):
  #     - avg模式: 所有patches平均池化为1个token per image
  #     - head_cam: 1相机 × 2时间步 × 1 token = 2 tokens
  #     - left_wrist_cam: 1相机 × 2时间步 × 1 token = 2 tokens
  #     - right_wrist_cam: 1相机 × 2时间步 × 1 token = 2 tokens
  #   
  #   ✅ 触觉传感器 (ResNet18 + GlobalAvgPool):
  #     - 触觉图经过ResNet18提取特征，GlobalAvgPool聚合为1个token per image
  #     - left_tactile: 1传感器 × 2时间步 × 1 token = 2 tokens (与腕部相机时间步对齐)
  #     - right_tactile: 1传感器 × 2时间步 × 1 token = 2 tokens (与腕部相机时间步对齐)
  #   
  #   ✅ 本体感知:
  #     - agent_pos投影后: 2时间步 = 2 tokens
  #   
  #   总计: visual_cond_len = 2 + 2 + 2 + 2 + 2 + 2 = 12 tokens
  visual_cond_len: 12  # 🔥 head(2) + left_wrist(2) + right_wrist(2) + left_tactile(2) + right_tactile(2) + proprio(2)
  
  max_lang_cond_len: 1024
  qkv_bias: true
  qk_norm: true

  language_conditioned: false
  pre_norm_modality: false

  # 🔥 Token级别MoE配置 (SparseMoeBlock - 细粒度路由)
  # 核心改进:
  # 1. Token级别路由：每个token独立选择专家，细粒度特征处理
  # 2. 专家自动学习：自动学习不同模态token的特征处理
  # 3. 🆕 梯度累积友好：跨micro-batch累积专家统计，负载均衡更稳定
  # 4. 高效并行调度：训练时专家并行处理，推理时优化调度
  # 5. 🔥 Gate-Attention：Cross-attention输出通过可学习的gate调制（参考Qwen3）
  use_token_moe: true         # 🔥 Token级MoE (原use_modality_moe)
  num_experts: 6              
  num_experts_per_tok: 2
  n_shared_experts: 1
  moe_aux_loss_alpha: 0.5  # 🔥 提高到0.5以增强gate梯度 (12层深模型需要更强信号)
  gate_type: 'headwise'    # 🔥 Gate-Attention类型: 'none', 'headwise', 'elementwise'（参考Qwen3）

  # 🔥 模态嵌入配置
  use_modality_embedding: true
  
  # 🔥 模态长度配置 (avg模式 - 各模态独立输出)
  # - head_cam: 1相机 × 2时间步 × 1 token = 2 tokens
  # - wrist: (left_wrist_cam + right_wrist_cam) × 2T × 1 token + (left_tactile + right_tactile) × 2T × 1 token = 8 tokens
  #         (各模态独立输出，时间步对齐)
  # - proprio: agent_pos × 2时间步 = 2 tokens (投影到RGB特征维度)
  # ⚠️ context_c结构: [head_avg_t0, head_avg_t1, 
  #                    left_wrist_avg_t0, left_wrist_avg_t1,
  #                    right_wrist_avg_t0, right_wrist_avg_t1,
  #                    left_tactile_t0, left_tactile_t1,
  #                    right_tactile_t0, right_tactile_t1,
  #                    proprio_t0, proprio_t1] 
  head_cond_len: 2   # 🔥 头部相机总token数 (1相机 × 2T × 1)
  wrist_cond_len: 8  # 🔥 腕部视觉(4=2相机×2T×1) + 触觉(4=2传感器×2T×1) 总token数

  flow_batch_ratio: 0.75 
  consistency_batch_ratio: 0.25

  sample_t_mode_flow: "beta"
  sample_t_mode_consistency: "discrete" 
  sample_dt_mode_consistency: "uniform"
  sample_target_t_mode: "relative"
  denoise_timesteps: 10

  diffusion_timestep_embed_dim: 128
  diffusion_target_t_embed_dim: 128

  horizon: ${horizon}
  n_action_steps: ${n_action_steps}
  n_obs_steps: ${n_obs_steps}

  num_inference_steps: 10
  obs_as_global_cond: true
  shape_meta: ${shape_meta}

  obs_encoder:
    _target_: maniflow.model.vision_2d.timm_multimodal_encoder.TimmMultimodalEncoder
    shape_meta: ${shape_meta}

    ##### RGB相机配置 (SigLIP) #####
    model_name: 'vit_base_patch16_siglip_224'  # SigLIP Base 86M
    pretrained: true
    frozen: true
    global_pool: ''
    feature_aggregation: 'avg'  # siglip/clip:'all_tokens'or'cls'or'avg'
    downsample_ratio: 32

    ##### 触觉传感器配置 #####
    tactile_model_name: 'resnet18'
    tactile_pretrained: false
    tactile_frozen: false
    tactile_feature_dim: 768  # 与SigLIP Base RGB特征维度一致
    share_tactile_model: true  # 左右手触觉共享权重
    tactile_output_all_patches: true  # 🔥 触觉也输出所有patch tokens (16x32触觉图下采样32倍后约0.5x1像素)

    ##### 🔥 输出token序列模式 (用于模态级MoE) #####
    output_token_sequence: true

    transforms:
      - type: RandomCrop
        ratio: 0.95
      - _target_: torchvision.transforms.RandomRotation
        degrees:
          - -5.0
          - 5.0
        expand: false
      - _target_: torchvision.transforms.ColorJitter
        brightness: 0.3
        contrast: 0.4
        saturation: 0.5
        hue: 0.08
     
    use_group_norm: false
    share_rgb_model: false
    imagenet_norm: true

ema:
  _target_: maniflow.model.diffusion.ema_model.EMAModel
  update_after_step: 0
  inv_gamma: 1.0
  power: 0.75
  min_value: 0.0
  max_value: 0.9999

dataloader:
  batch_size: 32  # 🔥 batch=1可能导致维度问题,改回2
  num_workers: 8  # 🔥 增加workers加速数据加载
  shuffle: True
  pin_memory: True  # 🔥 启用pin_memory加速GPU传输
  persistent_workers: True  # 🔥 保持worker进程，避免重复启动开销
  # prefetch_factor: 2  # 🔥 预取2个batch

val_dataloader:
  batch_size: 32  # 🔥 与训练保持一致
  num_workers: 4
  shuffle: False
  pin_memory: True
  persistent_workers: False

optimizer:
  _target_: torch.optim.AdamW
  lr: 1.0e-4
  betas: [0.9, 0.95]
  eps: 1.0e-8 
  weight_decay: 1.0e-3

training:
  device: "cuda:0"
  env_device: "cuda:0"
  seed: 42
  debug: False
  resume: True
  lr_scheduler: cosine
  lr_warmup_steps: 500
  num_epochs: 201
  
  use_wandb: true
  
  # 🔥 梯度裁剪配置 (解决MoE梯度爆炸问题)
  max_grad_norm: 10.0  # 🔥 放宽到10.0，避免裁剪抑制gate梯度
  
  use_ema: True  
  rollout_every: 100
  checkpoint_every: 100
  val_every: 50
  sample_every: 5
  max_train_steps: null
  max_val_steps: null
  tqdm_interval_sec: 1.0

logging:
  group: ${exp_name}
  id: null
  mode: online
  name: ${training.seed}
  project: ManiFlow_2d_SigLIP_ModalityMoE_Tactile
  resume: true
  tags:
  - ${logging.project}
  - timm_siglip_86m
  - token_moe  # 🔥 Token级MoE（细粒度路由）
  - gate_attention  # 🔥 Gate-Attention（参考Qwen3）
  - tactile_tokens  # 🔥 触觉时间步对齐
  - avg_pooling  # 🔥 RGB平均池化
  - expert_learning  # 🔥 专家自动学习
  - triple_camera

checkpoint:
  save_ckpt: True
  topk:
    monitor_key: val_loss
    mode: min
    k: 1
    format_str: 'epoch={epoch:04d}-val_loss={val_loss:.6f}.ckpt'
  
  save_last_ckpt: True
  save_last_snapshot: False
  save_every: 50

multi_run:
  run_dir: data/outputs/${now:%Y.%m.%d}/${now:%H.%M.%S}_${name}_${task_name}
  wandb_name_base: ${now:%Y.%m.%d-%H.%M.%S}_${name}_${task_name}

hydra:
  job:
    override_dirname: ${name}
  run:
    dir: data/outputs/${now:%Y.%m.%d}/${now:%H.%M.%S}_${name}_${task_name}
  sweep:
    dir: data/outputs/${now:%Y.%m.%d}/${now:%H.%M.%S}_${name}_${task_name}
    subdir: ${hydra.job.num}

checkpoint_num: 500
expert_data_num: 50
raw_task_name: none
setting: none

