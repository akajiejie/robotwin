defaults:
  - task: demo_task_tactile

name: train_maniflow_modality_moe_tactile_image_timm_policy_accelerate

task_name: ${task.name}
shape_meta: ${task.shape_meta}
exp_name: "debug"

horizon: 16
n_obs_steps: 2
n_action_steps: 16
n_latency_steps: 0
dataset_obs_steps: ${n_obs_steps}
keypoint_visible_rate: 1.0
obs_as_global_cond: true

policy:
  _target_: maniflow.policy.maniflow_image_policy.ManiFlowTransformerImagePolicy
  block_type: "DiTXMoE"  # 使用DiTXMoE (模态级别MoE)
  n_layer: 12
  n_head: 8
  n_emb: 768
  
  # 🔥 视觉条件长度配置 (output_token_sequence=true + all_tokens模式)
  # TimmMultimodalEncoder + output_token_sequence=true + feature_aggregation='all_tokens' 输出:
  #   ✅ RGB相机 (SigLIP vit_base_patch16_224):
  #     - 224×224 / 16×16 patch = 14×14 = 196 patches per image
  #     - head_cam: 1相机 × 2时间步 × 196 patches = 392 tokens
  #     - left_wrist_cam: 1相机 × 2时间步 × 196 patches = 392 tokens
  #     - right_wrist_cam: 1相机 × 2时间步 × 196 patches = 392 tokens
  #   
  #   ✅ 触觉传感器 (ResNet18 + output_all_patches=true):
  #     - 16×32 触觉图 / 32倍下采样 = 1×1 = 1 patch per tactile
  #     - left_tactile: 1传感器 × 1 patch = 1 token
  #     - right_tactile: 1传感器 × 1 patch = 1 token
  #   
  #   ✅ 本体感知:
  #     - agent_pos投影后: 2时间步 = 2 tokens
  #   
  #   总计: visual_cond_len = 392 + 392 + 392 + 1 + 1 + 2 = 1180 tokens
  visual_cond_len: 1180  # 🔥 head(392) + left_wrist(392) + right_wrist(392) + left_tactile(1) + right_tactile(1) + proprio(2)
  
  max_lang_cond_len: 1024
  qkv_bias: true
  qk_norm: true

  language_conditioned: false
  pre_norm_modality: false

  # 🔥 Token级别MoE配置 (SparseMoeBlock - 细粒度路由)
  # 核心改进:
  # 1. Token级别路由：每个token独立选择专家，细粒度特征处理
  # 2. 专家自动学习：在不同扩散时间步下自动关注不同模态的token特征
  # 3. 时间条件感知：门控根据扩散阶段调整专家选择偏好
  # 4. AdaLN协调：context_c在进入MoE前感知时间条件
  # 5. 🆕 梯度累积友好：跨micro-batch累积专家统计，负载均衡更稳定
  # 6. 高效并行调度：训练时专家并行处理，推理时优化调度
  # 7. 🔥 Gate-Attention：Cross-attention输出通过可学习的gate调制（参考Qwen3）
  use_token_moe: true         # 🔥 Token级MoE (原use_modality_moe)
  num_experts: 4              
  num_experts_per_tok: 2
  n_shared_experts: 1
  moe_aux_loss_alpha: 0.01
  enable_grad_accumulation: true  # 🔥 启用梯度累积友好模式 (gradient_accumulate_every=2时必须开启)
  gate_type: 'headwise'    # 🔥 Gate-Attention类型: 'none', 'headwise', 'elementwise'（参考Qwen3）

  # 🔥 模态嵌入配置
  use_modality_embedding: true
  
  # 🔥 模态长度配置 (all_tokens模式 - 各模态独立输出)
  # - head_cam: 1相机 × 2时间步 × 196 patches = 392 tokens
  # - wrist: (left_wrist_cam + right_wrist_cam) × 2T × 196 patches + (left_tactile + right_tactile) × 1 patch = 785 tokens
  #         (各模态独立输出，不做交叉注意力融合)
  # - proprio: agent_pos × 2时间步 = 2 tokens (投影到RGB特征维度)
  # ⚠️ context_c结构: [head_196patches_t0, head_196patches_t1, 
  #                    left_wrist_196patches_t0, left_wrist_196patches_t1,
  #                    right_wrist_196patches_t0, right_wrist_196patches_t1,
  #                    left_tactile_1patch, right_tactile_1patch,
  #                    proprio_t0, proprio_t1]
  head_cond_len: 392   # 🔥 头部相机总token数 (1相机 × 2T × 196)
  wrist_cond_len: 786  # 🔥 腕部视觉(784=2相机×2T×196) + 触觉(2=2传感器×1patch) 总token数

  flow_batch_ratio: 0.75 
  consistency_batch_ratio: 0.25

  sample_t_mode_flow: "beta"
  sample_t_mode_consistency: "discrete" 
  sample_dt_mode_consistency: "uniform"
  sample_target_t_mode: "relative"
  denoise_timesteps: 10

  diffusion_timestep_embed_dim: 128
  diffusion_target_t_embed_dim: 128

  horizon: ${horizon}
  n_action_steps: ${n_action_steps}
  n_obs_steps: ${n_obs_steps}

  num_inference_steps: 10
  obs_as_global_cond: true
  shape_meta: ${shape_meta}

  obs_encoder:
    _target_: maniflow.model.vision_2d.timm_multimodal_encoder.TimmMultimodalEncoder
    shape_meta: ${shape_meta}

    ##### RGB相机配置 (SigLIP) #####
    model_name: 'vit_base_patch16_siglip_224'  # SigLIP Base 86M
    pretrained: true
    frozen: false
    global_pool: ''
    feature_aggregation: 'all_tokens'  # siglip/clip:'all_tokens'or'cls'or'avg'
    downsample_ratio: 32

    ##### 触觉传感器配置 #####
    tactile_model_name: 'resnet18'
    tactile_pretrained: false
    tactile_frozen: false
    tactile_feature_dim: 768  # 与SigLIP Base RGB特征维度一致
    share_tactile_model: true  # 左右手触觉共享权重
    tactile_output_all_patches: true  # 🔥 触觉也输出所有patch tokens (16x32触觉图下采样32倍后约0.5x1像素)

    ##### 🔥 输出token序列模式 (用于模态级MoE) #####
    output_token_sequence: true

    transforms:
      - type: RandomCrop
        ratio: 0.95
      - _target_: torchvision.transforms.RandomRotation
        degrees:
          - -5.0
          - 5.0
        expand: false
      - _target_: torchvision.transforms.ColorJitter
        brightness: 0.3
        contrast: 0.4
        saturation: 0.5
        hue: 0.08
     
    use_group_norm: false
    share_rgb_model: false
    imagenet_norm: true

ema:
  _target_: maniflow.model.diffusion.ema_model.EMAModel
  update_after_step: 0
  inv_gamma: 1.0
  power: 0.75
  min_value: 0.0
  max_value: 0.9999

# 🔥 Accelerate配置 (触觉MoE可能需要更多内存)
dataloader:
  batch_size: 16  # 🔥 batch=1可能导致维度问题,改回2
  num_workers: 4  # 🔥 改为0避免多进程复制数据
  shuffle: True
  pin_memory: False  # 🔥 关闭pin_memory节省内存
  persistent_workers: False  # 🔥 num_workers=0时此项无效

val_dataloader:
  batch_size: 16  # 🔥 与训练保持一致
  num_workers: 2
  shuffle: False
  pin_memory: False
  persistent_workers: False

optimizer:
  _target_: torch.optim.AdamW
  lr: 1.0e-4
  betas: [0.9, 0.95]
  eps: 1.0e-8 
  weight_decay: 1.0e-3

training:
  device: "cuda:0"
  env_device: "cuda:0"
  seed: 42
  debug: False
  resume: True
  lr_scheduler: cosine
  lr_warmup_steps: 500
  num_epochs: 201
  
  # 🔥 Accelerate配置
  use_accelerate: true
  gradient_accumulate_every: 4  # 🔥 等效batch size = 2×16 = 32 (保持训练效果)
  mixed_precision: "no"  # 🔥 启用FP16混合精度,节省50%内存 (~30-40GB)
  use_wandb: true
  
  use_ema: True  
  rollout_every: 100
  checkpoint_every: 100
  val_every: 50
  sample_every: 5
  max_train_steps: null
  max_val_steps: null
  tqdm_interval_sec: 1.0

logging:
  group: ${exp_name}
  id: null
  mode: online
  name: ${training.seed}
  project: ManiFlow_2d_SigLIP_ModalityMoE_Tactile_Accelerate
  resume: true
  tags:
  - ${logging.project}
  - timm_siglip_86m
  - token_moe  # 🔥 Token级MoE（细粒度路由）
  - gate_attention  # 🔥 Gate-Attention（参考Qwen3）
  - tactile_patches  # 🔥 触觉patch tokens
  - all_tokens  # 🔥 RGB所有tokens
  - time_cond_aware  # 🔥 时间条件感知
  - expert_learning  # 🔥 专家自动学习
  - accelerate
  - triple_camera

checkpoint:
  save_ckpt: True
  topk:
    monitor_key: val_loss
    mode: min
    k: 1
    format_str: 'epoch={epoch:04d}-val_loss={val_loss:.6f}.ckpt'
  
  save_last_ckpt: True
  save_last_snapshot: False
  save_every: 50

multi_run:
  run_dir: data/outputs/${now:%Y.%m.%d}/${now:%H.%M.%S}_${name}_${task_name}
  wandb_name_base: ${now:%Y.%m.%d-%H.%M.%S}_${name}_${task_name}

hydra:
  job:
    override_dirname: ${name}
  run:
    dir: data/outputs/${now:%Y.%m.%d}/${now:%H.%M.%S}_${name}_${task_name}
  sweep:
    dir: data/outputs/${now:%Y.%m.%d}/${now:%H.%M.%S}_${name}_${task_name}
    subdir: ${hydra.job.num}

checkpoint_num: 500
expert_data_num: 50
raw_task_name: none
setting: none

